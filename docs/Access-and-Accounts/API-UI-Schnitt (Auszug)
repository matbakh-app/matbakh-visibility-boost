12) API-/UI-Schnitt (Auszug)

Backend Endpunkte

POST /orgs (create), GET /orgs/:id

POST /orgs/:id/invite (role, scope)

POST /content (draft), POST /content/:id/submit, POST /content/:id/approve

POST /vouchers (create), POST /vouchers/redeem

POST /test-accounts/invite, POST /test-accounts/:id/upgrade, DELETE /test-accounts/:id

GET /audit?scope=…

UI Kernseiten

Onboarding Wizard (Solo, Multi-Standort, Multi-Marke)

Team & Rollen

Freigaben (Approvals)

Integrationen (Meta, Google, später Gemini Computer Use)

Gutscheine & Test-Konten

Datenschutz & Storytelling-Freigaben