{
  "type": "object",
  "properties": {
    "USE_BEDROCK": {
      "type": "string",
      "enum": ["true", "false"],
      "default": "false",
      "description": "Enable AWS Bedrock AI for visibility analysis"
    },
    "BEDROCK_ENDPOINT": {
      "type": "string",
      "format": "uri",
      "description": "AWS Bedrock API endpoint URL"
    },
    "AWS_ACCESS_KEY_ID": {
      "type": "string",
      "description": "AWS access key for Bedrock service"
    },
    "AWS_SECRET_ACCESS_KEY": {
      "type": "string",
      "description": "AWS secret access key for Bedrock service"
    },
    "AWS_REGION": {
      "type": "string",
      "default": "eu-west-1",
      "description": "AWS region for Bedrock service"
    },
    "SUPABASE_URL": {
      "type": "string",
      "format": "uri",
      "description": "Supabase project URL"
    },
    "SUPABASE_ANON_KEY": {
      "type": "string",
      "description": "Supabase anonymous key"
    },
    "SUPABASE_SERVICE_ROLE_KEY": {
      "type": "string",
      "description": "Supabase service role key for edge functions"
    }
  },
  "required": [
    "SUPABASE_URL",
    "SUPABASE_ANON_KEY",
    "SUPABASE_SERVICE_ROLE_KEY"
  ],
  "additionalProperties": true
}