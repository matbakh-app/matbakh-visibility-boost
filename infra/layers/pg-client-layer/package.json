{
  "name": "pg-client-layer",
  "version": "1.0.0",
  "description": "Reusable PostgreSQL client layer with RDS secrets integration",
  "main": "index.js",
  "dependencies": {
    "pg": "^8.11.1",
    "@aws-sdk/client-secrets-manager": "^3.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.2",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc",
    "install-deps": "npm install --production"
  }
}