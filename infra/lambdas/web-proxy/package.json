{
  "name": "web-proxy",
  "version": "1.0.0",
  "description": "Lambda proxy for controlled web access from Bedrock AI agents",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "package": "npm run build && zip -r web-proxy.zip dist/ node_modules/",
    "deploy": "./deploy.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.5"
  },
  "keywords": [
    "aws",
    "lambda",
    "proxy",
    "web-access",
    "security",
    "matbakh"
  ],
  "author": "matbakh.app",
  "license": "UNLICENSED"
}